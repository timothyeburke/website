
## Customize dependencies
dependencies:
  pre:
    - npm install -g ember-cli
    - npm install
    - bower install

  override:
    - sudo pip install awscli

test:
  override:
    - ember build

deployment:
  aws:
    branch: master
    commands:
      - aws s3 sync dist/ s3://timburke.co/
